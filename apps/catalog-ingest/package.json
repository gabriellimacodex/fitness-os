{
  "name": "@fitness-os/catalog-ingest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fitness-os/domain": "workspace:*",
    "@fitness-os/schemas": "workspace:*"
  }
}
