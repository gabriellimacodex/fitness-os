# End-to-End Tests

This directory reserves the future Playwright boundary. Epic 00 relies on focused Vitest smoke tests and does not add a browser suite whose maintenance cost would exceed its current architectural value.

When a real cross-application journey exists, introduce Playwright here with an explicit test-server contract and CI ownership.
