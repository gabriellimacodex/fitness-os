# Copy this file to a local .env file when configuration is introduced.
# Never commit real credentials or environment-specific values.

# Drizzle Kit only. Never a real credential.
# DATABASE_URL=postgresql://user:pass@localhost:5432/fitness_os
